package org.usfirst.lib6647.util;

/**
 * Enum listing possible movement directions, for readability purposes.
 */
public enum Direction {
    UP, DOWN, FRONT, BACK, LEFT, RIGHT, FORWARD, BACKWARD, IN, OUT
}